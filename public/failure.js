if (window.innerWidth > 832) {
  setTimeout(function() {
    location.reload();
  });
}

$(window).resize(function() {
  if (window.innerWidth > 832) {
    setTimeout(function() {
      location.reload();
    });
  }
});
